# TripPilot AI 여행 플래너 — 포트폴리오 설명 문서

## 0. 한 줄 소개

서버 API 호출 없이, **브라우저에서 직접 추론하는 온디바이스 AI**로 여행 일정을 만들고 대화로 수정하는 여행 플래너.

---

## 1. 랜딩 페이지

**메뉴 구성**
비로그인 시 `기능 소개(#features)`, `AI 채팅 소개(#ai-chat)`, `커뮤니티`를 노출하고, 로그인 시 `내 일정`, `즐겨찾기`, `일정 만들기`가 추가된다. 로그인 여부와 저장된 일정 유무에 따라 Hero 영역이 3가지 버전(최근 일정 미리보기 / 빈 상태 CTA / 비로그인 샘플)으로 분기 렌더링된다.

**일정추가 진입점**
Hero, 기능 카드, CTA 등 여러 위치에서 아래 2번 항목의 두 메뉴(폼 방식 / AI 채팅 방식) 중 하나로 연결된다.

---

## 2. 일정추가 화면 — 메뉴 2개

### 메뉴 ① 일정 만들기 (폼 방식) — `/plan/new`
목적지, 기간, 인원, 예산, 숙소, 여행 스타일(관광/맛집/쇼핑/힐링/가족/커플/혼자)을 양식에 입력하면 몇 초 안에 일자별 일정표가 만들어진다. 목적지를 입력하고 포커스를 벗어나면, 같은 지역에 저장해둔 즐겨찾기 장소가 "필수 방문지"에 자동으로 병합되어 반복 입력을 줄여준다.

### 메뉴 ② AI와 대화하기 (채팅 방식) — `/plan/chat`
"3박 4일로 오사카 여행 갈 건데 3명이야, 맛집 위주로"처럼 자연스럽게 말하면, AI가 대화 속에서 목적지·기간·인원·예산·스타일을 하나씩 파악해 채워 넣는다. 필수 조건이 모두 채워지는 순간 자동으로 일정이 생성되어 상세 페이지로 이동한다.

### 두 메뉴의 관계
입력 방식만 다를 뿐, 조건이 다 채워지면 **동일한 일정 생성 엔진(`generatePlan`)**으로 결과를 만든다. 이 엔진 자체는 목적지·스타일에 맞는 장소 데이터를 조합하는 **결정론적 규칙 로직**이며 AI가 관여하지 않는다 — "AI 추천"이라는 표현은 UX 카피이고, 실제 AI가 개입하는 지점은 3~4번 항목의 **대화형 편집 기능**이다.

---

## 3. AI와 대화하는 화면 (일정 상세 페이지 채팅)

메뉴 ①/② 어느 쪽으로 일정을 만들었든, 이후 이동하는 **일정 상세 페이지에 공통으로 존재하는 채팅창**에서 "2일차에 디즈니랜드 추가해줘", "2일차에는 비가 온대" 같은 자연어 명령으로 일정을 수정할 수 있다.

### 정규식으로 처리되는 조건 (AI 미사용)

세 가지 명령(활동 추가/삭제/날씨 반영) 모두 공통 원칙: **"몇 일차인지"가 숫자로 명시되어 있고 + 미리 정의된 키워드가 포함되어야** 정규식이 성공한다. 둘 다 있어야 하는 AND 조건이며, 하나라도 없으면 실패한다.

| 명령 종류 | 필요 조건 | 인식 키워드 | 성공 예문 |
|---|---|---|---|
| 활동 추가 | 일차 숫자 + 동작 키워드 | 추가, 넣어, 포함 | "2일차에 디즈니 추가해줘" |
| 활동 삭제 | 일차 숫자 + 동작 키워드 | 삭제, 빼줘, 빼주, 제거, 없애 | "3일차 쇼핑 빼줘" |
| 날씨 반영 | 일차 숫자 + 날씨 키워드 | 비, 눈, 맑음/화창, 더워/폭염, 추워/한파, 태풍/폭풍, 미세먼지/황사, 실외/야외 | "2일차에는 비가 온대" |

조건을 만족하면 로컬 AI를 아예 로드하지 않고도 즉시(수 ms) 처리되며, 결과가 항상 예측 가능하다.

### 로컬 AI로 넘어가는 조건

판별 순서는 2단계다.
1. 동작 키워드가 문장에 있는가? → 없으면 즉시 AI로 넘어감
2. (1단계 통과 시) 일차 숫자가 문장에 있는가? → 없으면 AI로 넘어감

두 단계를 모두 통과해야만 정규식으로 처리가 끝난다. 즉 아래와 같은 경우 AI가 개입한다.

- **일차가 명시되지 않은 경우** — "디즈니 추가해줘"
- **지시어/대명사로 대체된 경우** — "거기다 그것도 넣어줘", "그날은 어떡하지"
- **동작 키워드 자체가 없는 경우** — "2일차 계획 좀 바꿔줘"
- **사전에 등록 안 된 표현인 경우** — "2일차 좀 눅눅할 것 같아"
- **여러 의도가 섞이거나 애매한 문장** — "내일이랑 모레 둘 다 비 온다는데"

이 경우 브라우저에서 로딩된 로컬 LLM(WebGPU, Qwen2.5-1.5B, `@mlc-ai/web-llm`)에 **현재 일정 전체 + 최근 대화 4턴**을 시스템 프롬프트로 넣고, `response_format: json_object`로 구조화된 응답(`{action, day, activity/weather}`)만 강제로 받는다.

### AI를 어디까지 신뢰하는가

의도 해석(AI, 비결정적)과 실제 일정 변경 실행(로직, 결정적)을 분리했다. AI가 존재하지 않는 일차나 활동을 가리켜도, 실행 단계에서 재검증을 거치므로 데이터가 깨지지 않는다.

---

## 4. AI를 통해 계획이 완성되는 과정

**날씨 반영 예시**: "비 온대"라고 말하면 그날의 실외 활동(관광/가족 스타일)만 골라 실내 활동(우선순위: 쇼핑 > 맛집 > 힐링 > 혼자 > 커플)으로 자동 교체한다. "다시 맑음"이라 하면 원래 계획으로 복구한다. 활동 히스토리를 계속 누적해 같은 날 동일 활동이 중복 추천되지 않는다.

**저장 시점**: 채팅이든 버튼 클릭이든, 일정이 바뀌는 즉시 서버에 저장된다(`PUT /api/trips/:id`). 별도의 "저장" 버튼은 없다. 채팅 응답 문구가 화면에 뜨는 시점엔 이미 서버 저장이 끝난 상태다.

---

## 5. 왜 "정규식 우선 + 로컬 AI 보조" 구조로 설계했는가

"처음부터 전부 AI로 처리해도 되지 않았을까?"에 대한 답. 실제 개발 중 겪은 트레이드오프를 코드 주석으로 남겨뒀다.

### 1) 속도 — 로컬 LLM은 생각보다 느리다
AI 응답 대기 타임아웃을 **15초 → 4초 → 1초**로 계속 줄였다. 가상 GPU 환경에서 실제로 써보니 몇 초만 기다려도 사용자에게는 "화면이 멈췄다"로 느껴졌기 때문이다. 정규식은 즉시(수 ms) 처리되므로, 명확한 문장까지 AI를 거치게 하면 불필요하게 느려진다.

### 2) 비용 — 서버 LLM은 대안이 아니었다
"서버 LLM으로 옮기면 빠르고 안정적이겠지만 비용이 발생하니 선택지에서 제외했다." 즉 "서버 AI(빠르지만 유료)" 대신 "로컬 AI(무료지만 느림)"를 택했고, 그 대신 호출 자체를 최소화(정규식 우선)하는 방향으로 속도 문제를 상쇄했다.

### 3) 정확도/예측 가능성 — 확실한 건 굳이 불확실하게 만들 이유가 없다
소형 로컬 모델(1.5B)은 완벽하지 않아 잘못된 값을 반환할 수 있다. 의미가 100% 명확한 문장은 정규식으로 고정해 항상 같은 결과를 보장하고, AI는 "규칙으로 못 푸는, 진짜 애매한 문장"에만 사용한다.

### 4) 호환성 — AI가 아예 안 뜨는 환경도 있다
WebGPU를 지원하지 않는 브라우저에서는 로컬 AI 로드 자체를 시도하지 않는다. 이런 환경에서도 활동 추가/삭제/날씨 반영 같은 핵심 기능은 동작해야 하므로, 정규식이 AI 유무와 무관하게 항상 동작하는 기본 보장선 역할을 한다.

> **요약**: AI를 안 쓴 게 아니라, 확실한 건 규칙으로 빠르고 안정적으로 처리하고, 정말 애매한 문장에만 제한된 리소스(로컬 모델)를 아껴 쓰는 방향으로 설계했다.

---

## 6. 기술 아키텍처 요약 (심화 / 면접 대비)

### 인증
JWT 기반 무상태(stateless) 인증. 로그인 시 서버가 `jwt.sign({sub: userId}, JWT_SECRET, {expiresIn: '7d'})`로 토큰을 발급하고, 프론트는 `localStorage`에 저장해 매 요청 `Authorization: Bearer` 헤더로 붙인다. 비밀번호는 Node 내장 `crypto.scrypt` + salt로 해시하고 `timingSafeEqual`로 타이밍 공격을 방어한다.

### 데이터 흐름 (일정 생성)
```
사용자 입력 → generatePlan(values)  ← 순수 함수, 네트워크 요청 없음
           → addTrip() → POST /api/trips
           → 서버: INSERT INTO trips(itinerary, form_values, history JSONB)
           → 응답으로 받은 trip.id로 /trips/:id 라우팅
```

### 데이터 흐름 (채팅으로 일정 수정)
```
resolveChatReply()
  1) intent 해석 (정규식 or 로컬 LLM)
  2) applyAddActivity / applyRemoveActivity / applyWeatherAction
     → 실행 전 "해당 일차·활동이 실제로 존재하는가" 재검증
  3) persistTripUpdate() → PUT /api/trips/:id
```
백엔드 PUT은 부분 업데이트가 아니라 풀 리소스 치환이라, `costs`/`times`를 body에 안 실으면 빈 값으로 덮어써버린다. 그래서 프론트는 itinerary만 바뀌는 요청에도 매번 `trip.costs`, `trip.times`를 함께 실어 보낸다 (실제로 겪은 이슈를 코드 주석으로 남겨둔 부분).

### 백엔드
Express + `pg`(ORM 없이 raw SQL, 파라미터 바인딩으로 SQL 인젝션 방지). `trips`/`favorites`/`community_trips` 등 주요 데이터는 PostgreSQL `JSONB` 컬럼에 저장해 스키마 변경 없이 구조를 유연하게 확장한다.

### 예상 꼬리질문 대비
- **왜 AI를 서버가 아니라 브라우저에서 돌렸나?** → 비용 0원 + 개인정보 미전송 + 정적 호스팅만으로 AI 기능까지 배포 가능
- **LLM 응답을 어떻게 신뢰하나?** → 신뢰하지 않는다. 실행 계층에서 항상 재검증, JSON 스키마 강제, 1초 타임아웃 + 규칙 기반 폴백
- **왜 PATCH 대신 PUT인가?** → 초기 설계 단순화 트레이드오프. 실제로 겪은 버그(costs/times 덮어쓰기)를 인지하고 있으며, 개선 방향으로 PATCH 리팩토링을 고려 중
